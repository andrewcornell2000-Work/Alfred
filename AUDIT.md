# Alfred audit notes

Historical audit content was superseded by the **installer overhaul** (work profile `core,powerbi,data`, skills → `~/.agents/skills`, per-project rules + graphify, doctor as success contract).

See [README.md](README.md) and [PACK.md](PACK.md) for the current product identity: catalog + platform-native provisioner only.

Verify a machine with:

```powershell
powershell -ExecutionPolicy Bypass -File Alfred-Doctor.ps1
```
