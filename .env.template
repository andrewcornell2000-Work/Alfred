# Alfred configuration — copy this to .env and fill in any values you need.
# Auth is handled by `claude login` and `codex login` — no API keys required.

# Optional: path to your Quant Intelligence System
# Only needed if Quant is not at the default location below.
# QUANT_PATH=C:\Users\yourname\Desktop\Quant
