# Vendored skill: impeccable

Design-language skill by pbakaus — audits/improves frontend UI.

- **Source:** https://github.com/pbakaus/impeccable (`.claude/skills/impeccable` build)
- **Version:** 3.8.0
- **Pinned commit:** c979ac37c361da564dcce100a4f2623d94ef54c8
- **Vendored:** 2026-06-29

This is the canonical (Claude) build. `Provision-Cursor.ps1` (`Sync-Impeccable`)
copies it to `~/.claude/skills/impeccable`, `~/.cursor/skills/impeccable`, and
`~/.codex/skills/impeccable`, rewriting the `.claude/skills/impeccable` script-path
prefix to match each harness, and wires the Cursor pre-edit detector hook into
`~/.cursor/hooks.json`.

## Updating

```
git clone --depth 1 --filter=blob:none --sparse https://github.com/pbakaus/impeccable.git
cd impeccable && git sparse-checkout set .claude/skills/impeccable
# copy .claude/skills/impeccable/* over skills/_vendor/impeccable/ (keep this file)
```

Then bump the version/commit above and re-run `Provision-Cursor.ps1`.
